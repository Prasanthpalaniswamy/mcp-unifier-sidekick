# Base64 MCP Server

A simple MCP (Model Context Protocol) server that provides tools to:

- Encode credentials to Base64
- Decode Base64 credentials

## Running locally (stdio)

```bash
python server.py